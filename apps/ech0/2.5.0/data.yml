additionalProperties:
  formFields:
    - default: 6277
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelZh: HTTP 端口
      description:
        zh: 应用对外提供 Web 服务的端口，范围 1-65535
      required: true
      rule: paramPort
      type: number
    - default: 6278
      edit: true
      envKey: PANEL_APP_PORT_SSH
      labelZh: SSH 端口
      description:
        zh: Ech0 内置 SSH/TUI 服务端口，用于终端管理
      required: true
      rule: paramPort
      type: number
    - default: ""
      envKey: PANEL_JWT_SECRET
      labelZh: JWT 密钥
      description:
        zh: 用于生成 JWT 的密钥，建议保留默认随机值或手动设置复杂字符串
      random: true
      required: true
      type: password
    - default: "ech0"
      envKey: CONTAINER_NAME
      labelZh: 容器名称
      description:
        zh: 容器名称，通常保持默认值即可
      required: true
      edit: false
      random: false
      rule: paramCommon
      type: text